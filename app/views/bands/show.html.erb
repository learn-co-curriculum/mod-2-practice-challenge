<h1><%= @band.name %></h1>
<p>Average age: <%= @band.average_age %></p>

<ul>
  <% @band.band_members.each do |member| %>
    <li><%= link_to member.musician.name, musician_path(member.musician) %> <%= member.instrument %></li>
  <% end %>
</ul>
